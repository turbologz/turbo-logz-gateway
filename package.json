{
  "name": "turbo-logz-gateway",
  "description": "Express Gateway Instance Bootstraped from Command Line",
  "repository": {},
  "license": "UNLICENSED",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "nodemon --exec node .",
    "test": "cross-env UI_URL=http://127.0.0.1:3000 GRAPHQL=http://127.0.0.1:3001 GRAPHQL_WS=http://127.0.0.1:3002  mocha -t 5000 --exit -r ts-node/register src/**/*.spec.ts test/**/*.spec.ts",
    "kube": "telepresence --swap-deployment test-unicron-gateway --run yarn start"
  },
  "dependencies": {
    "express-gateway": "^1.15.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/express": "^4.16.1",
    "@types/mocha": "^5.2.5",
    "@types/supertest": "^2.0.7",
    "chai": "^4.2.0",
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "mocha": "^5.2.0",
    "nodemon": "^1.18.7",
    "supertest": "^3.4.2",
    "ts-node": "^8.0.2",
    "typescript": "^3.3.3"
  },
  "nodemonConfig": {
    "ignore": [
      "node_modules"
    ],
    "ext": "json yml"
  }
}
